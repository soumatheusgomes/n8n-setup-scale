###############################
#         n8n‑STACK           #
#   Arquivo .env (exemplo)    #
###############################
# DICA: linhas começando com # são comentários.
# Copie este arquivo para `.env` e ajuste o que precisar.

######################################
# 1) Host / Protocolo
######################################
# Valor aceito em N8N_PROTOCOL:  http  ou  https
# • http   → exige NADA de domínio, bom para localhost ou VPS sem TLS
# • https  → requer perfil `tls` (Traefik) + domínio público válido
N8N_PROTOCOL=http

# Nome DNS público ou “localhost” / IP
# • Ex.: n8n.meusite.com   (cloud + TLS)
# • Ex.: 127.0.0.1         (localhost)
N8N_HOST=localhost

# E‑mail usado pelo Let’s Encrypt (somente se N8N_PROTOCOL=https)
EMAIL=admin@exemplo.com

# Timezone IANA (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
GENERIC_TIMEZONE=America/Sao_Paulo

######################################
# 2) Banco de dados PostgreSQL
######################################
# Duas opções:
# A) Banco remoto  → preencha HOST/USER/PASS
# B) Banco contêiner→ deixe POSTGRES_HOST vazio e use perfil `dblocal`
#
# Porta padrão do Postgres = 5432
POSTGRES_HOST=            # vazio = contêiner local
POSTGRES_PORT=5432
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=mudar_essa_senha   # ignorado se usar contêiner local

######################################
# 3) Browserless / Chrome headless
######################################
BROWSERLESS_TOKEN=demo-token-ou-prod-token

######################################
# 4) Outras variáveis (avançado)
######################################
# WEBHOOK_URL=              # se quiser forçar URL absoluta
# EXECUTIONS_DATA_PRUNE=true
# EXECUTIONS_DATA_MAX_AGE=168